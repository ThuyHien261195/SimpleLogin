<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardElevation="0dp"
    app:contentPadding="@dimen/item_profile_spacing">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center">

            <ImageView
                android:id="@+id/image_profile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:contentDescription="@string/description_profile_icon"
                android:src="@drawable/ic_profile" />

            <ImageButton
                android:id="@+id/image_button_delete_profile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="right"
                android:background="@android:color/transparent"
                android:src="@mipmap/ic_highlight_off_white_24dp"
                android:contentDescription="@string/description_delete_icon" />
        </FrameLayout>

        <TextView
            android:id="@+id/text_name"
            style="@style/LogInTextViewTheme"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/text_name_profile_vertical_spacing"
            android:ellipsize="end"
            android:maxLines="1"
            android:textSize="@dimen/tilte_name_text_size" />
    </LinearLayout>
</android.support.v7.widget.CardView>